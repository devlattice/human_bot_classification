
1) Materialize one labeled table for SSL
Join mixed_train.parquet + mixed_clusters.parquet on row index (same order as cluster_hdbscan).
Add columns: cluster, cluster_probability (if present), mix_source.
2) Define weights (don’t use cluster as hard label for validator)
Example policy (tune numbers):

mix_source == train_*: weight 1.0 (or keep as-is).
mix_source == validator:
cluster == -1: low weight (e.g. 0.2–0.5) — uncertain geometry.
cluster in {0,1}: base weight 0.3–0.7, then multiply by teacher confidence (next step).
3) Add teacher scores on validator rows
Run v4_ultralow_calibrated (or your chosen model) on robust validator features → p_bot.
Optional: high-confidence pseudo-label only if p_bot < 0.05 or > 0.95, else keep soft weight.
4) Build train parquet for lgbm_2
Labeled: original train_v2_robust (8000) — weight 1.0.
Unlabeled validator: only if your trainer supports it — usually you need labels for LightGBM, so either:
pseudo-label validator with low weight, or
duplicate / augment train with validator rows as a second domain with importance weights on real labels only (validator without labels → use reweighting / domain classifier path instead).
Simplest path that fits your stack: append validator rows with pseudo-label = cluster mapped to 0/1 only as initialization, but loss weight = w_teacher * w_cluster and cap validator fraction (e.g. ≤10–20% of batch).

5) Retrain + recalibrate + cross-eval
Train lgbm_2 with --sample-weight-col (you already have the hook).
Recalibrate on train_v2_robust/val.parquet (or a small labeled validator slice if you get one).
Cross-eval: PB + validator-shaped holdout + (optional) far holdout as stress test.
6) Freeze experiment metadata
Save mixed_clusters.json params + this confusion table + weight recipe in one place so reruns match.